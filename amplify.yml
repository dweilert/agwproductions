version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build step required - static site"
  artifacts:
    baseDirectory: /
    files:
      - 'index.html'
      - 'styles.css'
      - 'script.js'
      - 'assets/**/*'
  cache:
    paths: []
